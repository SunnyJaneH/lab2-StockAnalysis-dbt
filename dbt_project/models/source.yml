version: 2

sources:
  - name: raw
    database: USER_DB_SWORDFISH
    schema: raw
    description: "Schema containing raw stock price data (ETL)."
    tables:
      - name: stock_data
        identifier: stock_data
        description: "Raw stock price data including symbol, date, open, close, high, low, and volume."
